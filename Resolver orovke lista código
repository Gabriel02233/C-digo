
def sort_list(numbers):
    sorted_list = []
    while numbers:
        min_value = numbers[0]
        for num in numbers:
            if num < min_value:
                min_value = num
        sorted_list.append(min_value)
        numbers.remove(min_value)
    return sorted_list
